import { CheckPermissionDirectiveDirective } from './check-permission-directive.directive';

describe('CheckPermissionDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckPermissionDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
